diff --git a/Lib/Programs/Gbuffers/Entities_FS.glsl b/Lib/Programs/Gbuffers/Entities_FS.glsl
--- a/Lib/Programs/Gbuffers/Entities_FS.glsl
+++ b/Lib/Programs/Gbuffers/Entities_FS.glsl
@@ -324,4 +324,4 @@
 		clrwl_computeFragment(albedoTex, albedo, blockLight, ao, fragColor);
 
-		albedo.rgb *= fragColor.rgb;
+		albedo.rgb = mix(albedo.rgb, fragColor.rgb, fragColor.a);
 		blockLight.x = saturate(blockLight.x * 1.0666667 - 0.03333334);
diff --git a/Lib/Programs/Gbuffers/Water_FS.glsl b/Lib/Programs/Gbuffers/Water_FS.glsl
--- a/Lib/Programs/Gbuffers/Water_FS.glsl
+++ b/Lib/Programs/Gbuffers/Water_FS.glsl
@@ -138,4 +138,12 @@
 		vec3 dp1perp = cross(N, dp1);
-		vec3 T = normalize(dp2perp * duv1.x + dp1perp * duv2.x);
-		vec3 B = normalize(dp2perp * duv1.y + dp1perp * duv2.y);
+		vec3 T = dp2perp * duv1.x + dp1perp * duv2.x;
+		vec3 B = dp2perp * duv1.y + dp1perp * duv2.y;
+		#ifdef PROGRAM_COLORWHEEL
+			if (min(dot(T, T), dot(B, B)) == 0.0){
+				T = cross(abs(N.y) < 0.9 ? vec3(0.0, 1.0, 0.0) : vec3(1.0, 0.0, 0.0), N);
+				B = cross(N, T);
+			}
+		#endif
+		T = normalize(T);
+		B = normalize(B);
 		float invmax = inversesqrt(max(dot(T, T), dot(B, B)));
@@ -164,4 +172,4 @@
 		clrwl_computeFragment(albedoTex, albedo, blockLight, ao, fragColor);
 
-		albedo.rgb *= fragColor.rgb;
+		albedo.rgb = mix(albedo.rgb, fragColor.rgb, fragColor.a);
 		blockLight = vec2(0.0, saturate(blockLight.y * 1.0666667 - 0.03333334));
