package dev.engine_room.flywheel.iris.compile;

/**
 * Colorwheel wavelet OIT producer passes over a contract translucent program; the composite follows.
 */
public enum OitPass {
    DEPTH_RANGE,
    COEFFICIENTS,
    EVALUATE
}
